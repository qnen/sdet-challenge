package com.interview.tasks;

import java.util.HashMap;
import java.util.Iterator;
import java.util.Map;

import org.junit.Test;


public class TaskTen {

	@Test
	public void TaskTen() {
		
		//  Selenium is checking the locator before the page finishes loading, so the element doesn't exist yet
		//  A possible solution is to add an implicit wait for the element to become visible or interactive
		
	}
}
