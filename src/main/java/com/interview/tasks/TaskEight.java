package com.interview.tasks;

import static org.junit.Assert.assertTrue;

import org.junit.Test;


public class TaskEight {

	@Test
	public void TaskEight() {
		
		// We should select each number and divide it by 2, if it has any remainder then the number is odd, if there is no remainder then the number is even and we could delete it by clicking the button
		
	}
}
